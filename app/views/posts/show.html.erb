<div class="container my-5 post-show">
  <div class="mb-3">
    <%= link_to "← 一覧に戻る", posts_path, class: "text-decoration-none" %>
  </div>

  <div class="card shadow-sm p-4">
    <div class="d-flex align-items-center gap-3 mb-3">
      <div class="avatar-lg">
        <div class="avatar-fallback"><%= @post.user.name.first.upcase rescue "U" %></div>
      </div>
      <div>
        <div class="fw-bold fs-5"><%= @post.user.name %></div>
        <div class="text-muted small"><%= time_ago_in_words(@post.created_at) %>前</div>
      </div>
    </div>

    <h1 class="detail-title mb-3">「<%= @post.title %>」</h1>
    <div class="text-center text-muted mb-3">
      <% if @post.location.present? %>
        <%= @post.location %>
      <% end %>
    </div>

    <% if @post.image.attached? %>
      <div class="detail-image mb-3">
        <%= image_tag @post.image.variant(resize_to_limit: [1000, 600]), class: "detail-image-img" %>
      </div>
    <% end %>

    <div class="card mt-3">
      <div class="card-body">
        <%= simple_format(@post.text) %>
      </div>
    </div>

    <div class="d-flex justify-content-end gap-3 mt-3 text-muted">
      <div>💬 <%= @post.comments.count %> コメント</div>
      <div>❤️ <%= @post.favorites.count %> いいね</div>
    </div>

    <% if @post.user == current_user %>
      <div class="d-flex justify-content-end mt-3">
      <%= link_to "✏️ 編集", edit_post_path(@post), class: "btn btn-outline-primary btn-sm" %>
      </div>
    <% end %>
  </div>
</div>
