<!DOCTYPE html>
<html>
  <head>
    <title>Musiulyics</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Homemade+Apple&display=swap" rel="stylesheet">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %> 
      <header>
        <div>
          <%= link_to root_path do %>
            <%= image_tag "musiclyrics-horizontal.png", class: "header-logo", alt: "MusicLyrics" %>
          <% end %>
        </div>
        <div><%= link_to root_path, class: "text-white nav-link" do %>Musiulyics<% end %></div>
        <div><%= link_to "TOP", root_path, class: "text-white nav-link" %></div>
        <div><%= link_to "NEW POST", new_post_path, class: "text-white nav-link" %></div>
        <div><%= link_to "HOME", posts_path, class: "text-white nav-link" %></div>
        <div><%= link_to "DRAFT", confirm_posts_path, class: "text-white nav-link" %></div>
        <div><%= link_to "MEMBER", users_path, class: "text-white nav-link" %></div>
        <div><%= link_to "MY PAGE", user_path(current_user.id), class: "text-white nav-link" %></div>
        <div><%= link_to "log out", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white nav-link" %></div>
      </header>
    <% end %>  
    <%= yield %>
  </body>
</html>